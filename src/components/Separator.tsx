import * as DropdownMenu  from "@radix-ui/react-dropdown-menu";

export const Separator = () => {
    return (
        <DropdownMenu.Separator className="w-full bg-zinc-300 dark:bg-zinc-600 h-[1px]"/>
    );
};